// import React from 'react';
// import PropTypes from 'prop-types';
// import icons from '../../data/img/seacreatures';

// const iconNames = Object.keys(icons);

const propTypes = {
  // icon: PropTypes.oneOf(iconNames).isRequired,
};

// const SeaCreaturesIcons = ({ icon }) => <><img src={icons[icon]} alt={icon} /></>;
const SeaCreaturesIcons = ({ icon }) => null;

SeaCreaturesIcons.propTypes = propTypes;

export default SeaCreaturesIcons;
